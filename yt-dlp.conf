# Download video up to 720p (fallback to best available if not)
-f "bv[height<=720]+ba/b[height<=720]/b"

# Output format and filename
-o "%(title)s.%(ext)s"

# Merge into MP4 container (video+audio)
--merge-output-format mp4
